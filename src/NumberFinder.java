public class NumberFinder {
        // Phương thức tìm số lớn nhất của hai số nguyên
        public static int findMax(int num1, int num2) {
            return (num1 > num2) ? num1 : num2;
        }
    }

